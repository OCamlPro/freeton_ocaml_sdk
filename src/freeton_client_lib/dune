; generated by drom from package skeleton 'library'

(library
  (name ton_client)
  (public_name freeton_client_lib)
  (wrapped true)
  (libraries freeton_types_lib freeton_ocaml_sdk )
  (foreign_stubs
    (language c)
    (names ton_client_ocaml_stubs)
    )
  (c_library_flags -lgsl)
  (preprocess (pps ppx_deriving_encoding))
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package freeton_client_lib))


