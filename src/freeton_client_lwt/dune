; generated by drom from package skeleton 'library'

(library
  (name ton_client_lwt)
  (public_name freeton_client_lwt)
  (wrapped true)
  (libraries lwt.unix freeton_client_lib )
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package freeton_client_lwt))


