; generated by drom from package skeleton 'library'

(library
  (name tON)
  (public_name freeton_base_lib)
  (wrapped true)
  (libraries zarith ezjsonm ez_file ez_api.encoding ez_api.cohttp_lwt cohttp-lwt-unix )
  
  (preprocess (pps ppx_deriving_encoding))
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package freeton_base_lib))


